{% extends "base.html.twig" %}
{% block body %}

    <h1>Adding a new product</h1>

    {{ form_start(form) }}
        {{ form_row(form.excel) }}
        <button type="submit" class="button">Upload</button>
    {{ form_end(form) }}
{% endblock %}
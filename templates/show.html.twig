{% extends "base.html.twig" %}
{% block body %}

    {% if usersInfo %}
        <h1>{{ usersInfo.lastname }}</h1>
        <p>{{ usersInfo.firstname }}</p>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="button">S'inscrire</button>
        {{ form_end(form) }}

    {% else %}
        Pas de données correspondantes.
    {% endif %}
{% endblock %}